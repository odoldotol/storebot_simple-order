# menu_item__item_option

- PK(store_id, item_id, order_int, item_option_id)
- UNIQUE(item_id, item_option_id)

store_id(store.id) [fk]

item_id(item.id) [fk]
item_option_id(item_option.id) [fk]

order_int(smallint)