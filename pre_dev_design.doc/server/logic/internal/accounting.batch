assignmentQueue 에 정산id 와 마감시간정보가 들어있음
하나씩 꺼내서 assignment 작업 수행
단, 일정시간 기준이 지나지 않은 정산id 는 타이머 예약

assignment 배치서버는 마감이 완료되어 생성된 정산 데이터의 정합성 검사를 수행함.

마감후 일정시간이 지난 정산 레코드를 정합성 검사함.
주문레코드, 결제정보, 주문스트림 3가지의 정합성을 검사.
이상이 없으면 정산레코드에 이상없음처리, 스트림에서 오더히스토리같은 정보 주문서DB에 업데이트.
스트림 제거.